# 修复后真实创作续验

日期：2026-09-10。作品：[prj_c0d2c7df](http://127.0.0.1:8788/projects/prj_c0d2c7df)。本轮未改产品代码，未公开发布、未开启运营；原验收报告及原项目保留。本报告是增量证据，不替换首次失败结论。

## 已实际完成

- 接续 UX-FIX-01 创建的只填想法作品，使用已保存的观点、动机及“不编造个人经历”访谈。资料采用本仓库三份已读取验收记录：review-04-regression.md、real-flow-results-20260910-2009.md、ux-blockers-fixed/README.md；属于本地项目案例，不冒充公开网页研究。
- 浏览器提交真实写稿，`gpt-5.6-sol` 返回 HTTP 200，标题《通过测试之后，作品就真的能写了吗？》。建议尚未采用时，原手工正文保持不变；审阅后通过编辑器填入并保存为主稿 v2，原稿保留历史。见 draft-response.json、draft-proposal.png。
- 浏览器真实请求 `gpt-image-2` 三次生成、一次基于原图编辑，均 HTTP 201。原封面 `vas_0438ebae`，插图 `vas_3e776177`、`vas_e563f745`，编辑封面 `vas_9c56f090` 的 reference_asset_id 指向原封面。原图保留，选用编辑版。见 images.json、edit.json 和候选截图。
- 浏览器创建微信、头条独立稿，分别改标题、保存 v2 并锁定。使用复制主稿路径，没有声称做过 AI 平台改写。两个只读预览均检查三张图片 complete 且 naturalWidth > 0；见 platform-0.png、platform-1.png。
- 明确选择公众号 main，绑定账号 acc_666cf35f。重新检查返回 ready=true、stale=false，快照为主稿 v2、两个平台稿 v2及三张所选图片。审批身份明确为“Codex 验收代理（非作者署名）”，备注限定草稿验收，不代表作者公开发布认可。

## 费用与内容边界

文本审计估算 $0.06226；四次图片返回各估算 $0.12，图片合计 $0.48。均非中转站实际账单。图片费用来自资产记录，不能冒称均记录在 llm_calls 或已验证账号预算扣款。界面画幅与实际返回尺寸不完全相同，不据此宣称严格 16:9。

目标读者仍显示“待确认”；本地来源链接也不适合直接对外发布。本文是链路验收稿，作者是否署名仍待本人决定。

## 用例状态（整项严格口径）

| 用例 | 状态 | 本次证据与剩余项 |
|---|---|---|
| T01 | NOT RUN（未完整重测） | 真实初稿、建议隔离及保存已验证；全程计时、真实生成期间重复点击及修改一句后的完整步骤未逐项复测。访谈是表单，不称为 AI 主动对话。 |
| T02 | NOT RUN（未完整重测） | 三图、原图编辑、版本保留、双平台图文预览实际成功；生成期间重复点击和全部放大交互未完整留证，不升级整项 PASS。 |
| T03 | NOT RUN（未完整重测） | 两平台独立标题、v2保存、锁定及代理审批已完成；恢复、上游更新、审批过期和手机全流程未在本轮重跑。 |
| T04 | FAIL（前端回执）；后台可见性待核验 | 实际草稿接口成功，但前端请求超时，没有收到成功显示；只提交一次，随后只读核对。 |
| T05 | FAIL（平台交付入口） | 页面仍明确仅支持头条预览、Markdown及ZIP；本轮没有第二个真实账号隔离验证，不绕过UI冒称交付成功。 |
| T06–T08 | NOT RUN | 未开启真实运营，未在生产环境注入故障。 |

## 使用摩擦

配图方向与额外提示为两个输入，预置额外提示可能保留旧方向；本次手工同时填写。视觉计划还要求填写风格说明。平台稿的编辑区 Markdown 预览与带资产图片的“只读预览”不同，用户需再开预览才能检查完整图文。交付记录下仍展示大量后置平台技术能力，信息密度问题仍在。

本轮仅增量真实验收，不沿用历史 1950 passed 作为新一轮测试结果；上次修复的 Python、浏览器与构建结果见 ../ux-blockers-fixed/README.md。QA-01 保持未完成。

## 公众号实际交付结果

浏览器只点击一次“送到草稿箱”。等待 response 180 秒未获得响应；该前端调用沿用 apiPost 默认 30 秒超时。随后只读查询 delivery-attempts，发现唯一记录 `da_2073ba36` 已成功，publication `p_3219f5f6`、content `c_0951123e`，mode=draft、outcome=success。

media_id：`T0wXMUxGNoEwkhzIN5QdEJBw4KBQtkZzyJTLOcWssxUPL9PGfrPsssebA4HM8cO0`。脱敏回执见 draft-delivery.json。没有重复发送，也没有公开发布。本次未再出现旧报告的 IP 白名单失败，但未登录公众号后台核对图文，因此后台可见性与图片完整性仍未验收。

待修复：草稿请求超时后应主动核对既有交付记录并显示成功或未知状态，避免用户误以为失败后重复点击；延长超时只能缓解，不能替代结果核对。
