# 第四次复审修复验证 — 2026-09-10

本轮范围为用户要求直接修复的五项缺陷。所有生成和平台调用使用测试替身，SQLite、作品 sidecar、预算持久化、文件锁和交付记录使用真实实现；未使用真实 key 请求模型，未向平台发送文章。

## 修复提交

| 提交 | 修复 |
| --- | --- |
| `d226a26` | 已有主稿恢复不再引用未赋值变量；重新检查主稿及平台稿，检查失败保留修改，不再写默认 pass/8 |
| `ff31dfa` | 账号级文件锁覆盖运营执行；活跃任务不可接管，终态不可重跑，交付前检查取消状态 |
| `5bd401d` | 请求前定价与原子预算预留；图片落盘后可恢复登记；费用幂等结算；未知请求禁止自动重发 |
| `b578d52` | 原始发送意图与最终回执关联；明确失败可显式重试；查询链终态可闭合；网络/查询异常及缺失回执保持 unknown |

## 回归结果

- Python：完整覆盖 `tests/` 下 137 个 `test_*.py` 文件，分 10 批，共 **1950 passed，0 failed**。
- 本机只有约 4 GB 内存且 swap 已满，按文件名排序、每 15 个文件启动一个新 pytest 进程，避免整套测试长进程的内存压力。每批运行 `.venv/bin/python -m pytest <files> -q --tb=short`，移除 HTTP/HTTPS/ALL 代理变量，设置 `MEDIAFORGE_OPS_TICK_SEC=0`。
- 新增 17 个用例；先确认原实现失败，再修复。覆盖恢复成功、坏主稿/平台稿、活跃任务竞争、终态保护、预算不足、未定价、登记失败恢复、未知计费请求、预留与幂等结算、明确失败重试、查询链失败恢复及三类不确定平台回执。
- 前端：`npm run build -- --outDir /tmp/mediaforge-fixed-build` 通过。只有已有的大 chunk 警告；未改前端源码或手工编辑 dist。
- `git diff --check` 通过；冻结 models、SQLite schema、Adapter 公共签名与 TECH_SPEC 未改动；Anthropic 导入护栏通过。

| 批次 | 文件数 | passed | 用时 |
| --- | --- | --- | --- |
| 1 | 15 | 106 | 7.2 s |
| 2 | 15 | 378 | 18.3 s |
| 3 | 15 | 242 | 15.6 s |
| 4 | 15 | 238 | 11.4 s |
| 5 | 15 | 236 | 77.0 s |
| 6 | 15 | 137 | 17.2 s |
| 7 | 15 | 224 | 10.7 s |
| 8 | 15 | 259 | 18.3 s |
| 9 | 15 | 121 | 8.8 s |
| 10 | 2 | 9 | 2.5 s |

## 恢复与费用说明

账号 `spend.json` 保留已有 `spent_usd`，新增 reservations/settled 保存预留与幂等结算。调度预算包含未结算预留；图片资产费用仍为配置单价的估算，不是平台账单。

每个自动图片槽位保存独立请求 sidecar。只有已确定未调用 provider 的预算拒绝会释放预留；超时等未知结果保留请求和额度，禁止盲目重发。已原子落盘的图片可在资产登记失败后继续登记，无需再次生图。

本轮没有重启服务、开启账号运营、公开发布或覆盖现有作品。真人两平台验收、最终署名仍属于 QA-01/R0，保持未完成，不用自动化回归代替。
