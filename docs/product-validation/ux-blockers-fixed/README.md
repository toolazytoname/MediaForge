# 创作阻塞修复后验证 — 2026-09-10

原始验收报告 `../real-flow-results-20260910-2009.md` 保留原状；本报告只记录修复后的局部复测，不覆盖首次失败结论。

## 修改

- 作品读取成功后，操作错误不再替换整个工作台；绑定 404/account_not_bound 是正常未绑定状态，其他错误提供重试并拦截交付。
- 公众号选择入口放到作品顶部，不依赖审批完成；不默认选号。绑定成功才更新已确认选择，失败保留旧选择并要求核对；旧项目的延迟响应不能改变新项目账号。
- 新建只需想法，其余为明确展示的可选默认设置；空想法中文行内提示，正在提交时不重复发送。灵感转项目也使用相同默认字段。
- 侧栏折叠时创建/设置使用带可访问名称和提示的图标按钮，不越出侧栏。

## 自动回归

- 修改前浏览器回归：4 failed / 2 passed；修改后增加保存失败、绑定失败、延迟绑定和重复提交，共 **10 passed**。
- 执行：`MEDIAFORGE_BROWSER_URL=http://127.0.0.1:8788 .venv/bin/python -m pytest tests/browser/test_creation_blockers.py -q`。
- 浏览器测试读取既有验收作品，创建/保存/绑定错误场景拦截 HTTP 请求，不修改原作品或账号。需要该本机验收数据与运行服务；未设置 URL 时显式跳过，不能当成浏览器验收成功。
- Python 常规全量：137 文件分 10 批 **1950 passed**。移除代理变量并关闭测试 tick，每批 15 个文件，防止本机长进程内存压力。
- 前端生产构建通过，dist 由构建生成；只有既有大 chunk 警告。`git diff --check` 通过。

## 真实页面走查

运行已有 `127.0.0.1:8788` 服务，使用新构建静态资源；没有重启后端或开启运营。agent-browser 命令在本机不可用，使用已安装的 Playwright 浏览器执行。

1. 仅输入想法，从 UI 创建 `prj_c0d2c7df`。默认读者为待确认，不要求绑定账号即可进入编辑器。
2. 在该新作品中填写并确认访谈，保存手工正文，刷新后核对正文一致。无真实模型调用。
3. 只读打开 `prj_cfc95b8c` 和 `prj_a63f79b2`，确认访谈与保存按钮可见；未修改这两个作品。
4. 在 390×844、768×844、1440×844 查看“今天”，按钮几何边界均位于侧栏内；已目视核对手机截图，无待办遮挡。
5. 全程无未捕获浏览器脚本异常，见 `result.json`。

证据：`new-project.png`、两个以项目 ID 命名的截图、`today-390.png`、`today-768.png`、`today-1440.png`。

## 原用例状态如何理解

| 用例 | 本轮局部结果 | 仍待执行 |
| --- | --- | --- |
| T01 | 只填想法创建、未绑定编辑、访谈确认、手工保存刷新通过 | 真实 AI 起稿与完整作者审阅未重测；访谈仍是表单 |
| T03 | 原作品可打开、手机侧栏不遮挡、保存失败保留输入通过 | 平台稿版本恢复与过期审批完整 UI 路径未重测 |
| T05 | 前置账号选择、绑定错误恢复及延迟响应不串账号通过 | 真实双账号交付与头条草稿入口/后台验收未执行 |
| T04 | 未执行新的平台请求 | 原报告的 IP 白名单阻塞需实际重新核对 |

T02/T06/T07/T08 未在本轮完成真实验收。QA-01 与 R0 保持未完成。未公开发布、未群发，无本轮付费请求；新验收作品保留以便下次继续。
